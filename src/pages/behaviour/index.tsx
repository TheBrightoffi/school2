import type { NextPage } from 'next'
import Head from 'next/head'

const RateBehaviours: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Rate Behaviours | School Management System</title>
      </Head>
      <h1>Rate Behaviours</h1>
      {/* Add behaviour rating interface here */}
    </div>
  )
}

export default RateBehaviours