import type { NextPage } from 'next'
import Head from 'next/head'

const CreateTimetable: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Create Timetable | School Management System</title>
      </Head>
      <h1>Create Timetable</h1>
      {/* Add timetable creation interface here */}
    </div>
  )
}

export default CreateTimetable