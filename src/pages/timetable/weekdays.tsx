import type { NextPage } from 'next'
import Head from 'next/head'

const WeekdaysSetup: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Weekdays Setup | School Management System</title>
      </Head>
      <h1>Weekdays Setup</h1>
      {/* Add weekdays setup interface here */}
    </div>
  )
}

export default WeekdaysSetup