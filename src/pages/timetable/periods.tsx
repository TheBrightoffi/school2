import type { NextPage } from 'next'
import Head from 'next/head'

const TimePeriodsSetup: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Time Periods Setup | School Management System</title>
      </Head>
      <h1>Time Periods Setup</h1>
      {/* Add time periods setup interface here */}
    </div>
  )
}

export default TimePeriodsSetup