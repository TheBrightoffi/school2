import type { NextPage } from 'next'
import Head from 'next/head'

const SalarySheet: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Salary Sheet | School Management System</title>
      </Head>
      <h1>Salary Sheet</h1>
      {/* Add salary sheet generation interface here */}
    </div>
  )
}

export default SalarySheet